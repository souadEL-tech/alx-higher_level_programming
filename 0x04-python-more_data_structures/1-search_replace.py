#!/usr/bin/python3

def search_replace(my_list, search, replace):

    # creat new list
    new_list = list()

    for i in my_list:
        if i == search:
            new_list.append(replace)
        else:
            new_list.appende(raplace)
    return (new_list)
