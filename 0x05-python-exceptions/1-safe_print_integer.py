#!/usr/bin/python3

def safe_print_integer(value):
    # check if val is an integer

    try:
        if valeu == int(valeu):
            print("{:d}".format(valeu))
            return (True)
    except Exception:
        return (False)
